# CORS origins (comma-separated if multiple)
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Optional: Directory to save scraped project data (defaults to backend/data)
# DATA_DIR=/path/to/custom/dir
